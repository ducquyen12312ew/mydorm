\documentclass[../DoAn.tex]{subfiles}

\begin{document}

\begin{center}
    {\bfseries\fontsize{18pt}{22pt}\selectfont
        DANH SÁCH THUẬT NGỮ}
\end{center}
\vspace{1cm}

\begin{longtable}{p{4.2cm} p{10.3cm}}
    \hline
    \textbf{Thuật ngữ} & \textbf{Ý nghĩa} \\
    \hline
    \endhead
    \textbf{Aggregation pipeline} & chuỗi xử lý tổng hợp dữ liệu nhiều bước trong MongoDB \\
    \textbf{Allocation engine}    & bộ máy phân bổ phòng tự động \\
    \textbf{Audit log}            & nhật ký ghi lại các thao tác quan trọng của hệ thống \\
    \textbf{Audit trail}          & vết kiểm toán cho phép truy nguyên toàn bộ thao tác \\
    \textbf{Authentication}       & xác thực danh tính người dùng \\
    \textbf{Authorization}        & phân quyền truy cập tài nguyên \\
    \textbf{Backend}              & tầng xử lý nghiệp vụ và dữ liệu phía máy chủ \\
    \textbf{Bcrypt}               & thuật toán băm mật khẩu một chiều \\
    \textbf{Bottleneck}           & điểm nghẽn hiệu năng hệ thống \\
    \textbf{Breakpoint}           & ngưỡng kích thước màn hình để thay đổi bố cục giao diện \\
    \textbf{Broadcast}            & phát một thông điệp đồng thời tới nhiều client \\
    \textbf{Brute-force}          & tấn công dò tìm mật khẩu bằng cách thử vét cạn \\
    \textbf{Build pipeline}       & quy trình tự động hóa build và kiểm thử \\
    \textbf{Callback}             & hàm được gọi lại sau khi tác vụ hoàn thành \\
    \textbf{Cluster mode}         & chế độ chạy đa tiến trình song song \\
    \textbf{Connection pool}      & vùng kết nối cơ sở dữ liệu dùng chung \\
    \textbf{Container}            & đơn vị đóng gói ứng dụng và môi trường chạy \\
    \textbf{Dashboard}            & bảng điều khiển tổng quan hệ thống \\
    \textbf{Decay factor}         & hệ số suy giảm trọng số trong dự báo \\
    \textbf{Deployment}           & quá trình triển khai phần mềm lên máy chủ \\
    \textbf{Edge case}            & tình huống biên hiếm gặp khi kiểm thử \\
    \textbf{Endpoint}             & điểm cuối của API nhận và xử lý request \\
    \textbf{Event-driven}         & kiến trúc xử lý theo sự kiện \\
    \textbf{Fallback}             & cơ chế dự phòng khi chức năng chính gặp lỗi \\
    \textbf{Framework}            & khung phát triển phần mềm có sẵn cấu trúc \\
    \textbf{Frontend}             & tầng giao diện hiển thị phía người dùng \\
    \textbf{Geocoding}            & chuyển đổi địa chỉ thành tọa độ địa lý \\
    \textbf{Globe 3D}             & mô hình địa cầu ba chiều hiển thị trên trình duyệt \\
    \textbf{Hash}                 & giá trị băm được tính từ dữ liệu đầu vào \\
    \textbf{Horizontal scaling}   & mở rộng hệ thống bằng cách thêm máy chủ \\
    \textbf{Hotspot}              & điểm tương tác trong môi trường thực tế ảo \\
    \textbf{Interceptor}          & lớp chặn để tự động xử lý request và response \\
    \textbf{Load balancer}        & bộ cân bằng tải giữa các máy chủ \\
    \textbf{Logging}              & ghi nhật ký hoạt động hệ thống \\
    \textbf{Middleware}           & lớp xử lý trung gian trong luồng request \\
    \textbf{Migration}            & quá trình chuyển đổi cấu trúc dữ liệu \\
    \textbf{Mobile-first}         & nguyên tắc thiết kế ưu tiên thiết bị di động \\
    \textbf{Model (3D)}           & tệp mô hình không gian ba chiều \\
    \textbf{Multi-stage build}    & quy trình build nhiều giai đoạn để tối ưu Docker image \\
    \textbf{Nearest-year method}  & phương pháp dự báo dùng năm gần nhất có độ lệch quota $\leq 5\%$ \\
    \textbf{Occupancy rate}       & tỉ lệ lấp đầy chỗ ở trong ký túc xá \\
    \textbf{Offline support}      & khả năng hoạt động khi không có kết nối mạng \\
    \textbf{Onboarding}           & luồng dẫn dắt người dùng mới làm quen hệ thống \\
    \textbf{Optimistic update}    & cập nhật giao diện trước khi máy chủ xác nhận \\
    \textbf{Overhead}             & chi phí tài nguyên phát sinh thêm \\
    \textbf{Payload}              & dữ liệu chính được truyền trong request hoặc response \\
    \textbf{Peak hours}           & giờ cao điểm có lưu lượng truy cập lớn nhất \\
    \textbf{Plugin}               & thành phần mở rộng chức năng có thể cắm thêm \\
    \textbf{Polling}              & cơ chế hỏi vòng định kỳ để lấy dữ liệu mới \\
    \textbf{Priority score}       & điểm ưu tiên tổng hợp để xếp hạng phân bổ \\
    \textbf{Pub/sub}              & cơ chế phát hành --- đăng ký để truyền sự kiện \\
    \textbf{Quota}                & chỉ tiêu số lượng sinh viên được phân bổ \\
    \textbf{Rate limiting}        & giới hạn số request trong một khoảng thời gian \\
    \textbf{Real-time}            & xử lý và cập nhật dữ liệu tức thì \\
    \textbf{Refresh token}        & token dùng để cấp lại access token đã hết hạn \\
    \textbf{Render}               & quá trình tạo ra giao diện hiển thị cho người dùng \\
    \textbf{Replica set}          & cụm bản sao cơ sở dữ liệu đảm bảo tính sẵn sàng \\
    \textbf{Responsive}           & giao diện tự động thích ứng với mọi kích thước màn hình \\
    \textbf{Reverse proxy}        & máy chủ trung gian tiếp nhận và chuyển tiếp yêu cầu \\
    \textbf{Rollback}             & khôi phục trạng thái dữ liệu về thời điểm trước đó \\
    \textbf{Roommate matching}    & ghép cặp sinh viên ở chung phòng phù hợp \\
    \textbf{Sandbox}              & môi trường thử nghiệm cô lập, không ảnh hưởng dữ liệu thật \\
    \textbf{Schema}               & cấu trúc định nghĩa dữ liệu trong cơ sở dữ liệu \\
    \textbf{Service worker}       & tập lệnh chạy nền hỗ trợ PWA hoạt động offline \\
    \textbf{Session}              & phiên làm việc lưu trạng thái người dùng trên máy chủ \\
    \textbf{Sharding}             & phân mảnh cơ sở dữ liệu ra nhiều node \\
    \textbf{Simulation}           & mô phỏng quy trình trên dữ liệu bản sao \\
    \textbf{Snapshot}             & ảnh chụp trạng thái dữ liệu tại một thời điểm \\
    \textbf{SSL termination}      & giải mã HTTPS tại biên trước khi chuyển tiếp nội bộ \\
    \textbf{State management}     & quản lý trạng thái của ứng dụng \\
    \textbf{Stateless}            & kiến trúc không lưu trạng thái phiên trên máy chủ \\
    \textbf{Stress test}          & kiểm thử hệ thống ở ngưỡng vượt quá giới hạn bình thường \\
    \textbf{Template engine}      & công cụ tạo HTML động từ dữ liệu và khuôn mẫu \\
    \textbf{Throughput}           & số lượng request hệ thống xử lý được trong một giây \\
    \textbf{Token}                & chuỗi mã hóa dùng để xác thực hoặc ủy quyền \\
    \textbf{Upload}               & tải tệp từ thiết bị người dùng lên máy chủ \\
    \textbf{Use case}             & trường hợp sử dụng mô tả tương tác giữa tác nhân và hệ thống \\
    \textbf{Virtual tour}         & tham quan không gian ảo qua môi trường 3D tương tác \\
    \textbf{Waitlist}             & danh sách chờ khi vượt quá chỉ tiêu phân bổ \\
    \textbf{Webhook}              & cơ chế gửi thông báo tự động khi có sự kiện \\
    \textbf{Weighted average}     & trung bình có trọng số trong tính toán dự báo \\
    \textbf{Wireframe}            & bản phác thảo bố cục giao diện ở giai đoạn thiết kế \\
    \hline
\end{longtable}

\end{document}
